{# 年月から年度（4月始まり）と年・月を導く。原典は年度単位のファイルに月次シートが
   並ぶ形で、年度そのものを持つ列は無い。 #}

select
    cast(left(year_month, 4) as integer) as year,
    cast(right(year_month, 2) as integer) as month,
    case
        when cast(right(year_month, 2) as integer) >= 4
            then cast(left(year_month, 4) as integer)
        else cast(left(year_month, 4) as integer) - 1
    end as fiscal_year,
    year_month,
    operator_name,
    is_retail,
    is_general_transmission_distribution,
    is_transmission,
    is_distribution,
    is_specified_transmission_distribution,
    is_generation,
    is_specified_wholesale,
    hydro_conventional_plants,
    hydro_conventional_capacity_kw,
    hydro_pumped_storage_plants,
    hydro_pumped_storage_capacity_kw,
    hydro_plants,
    hydro_capacity_kw,
    thermal_coal_plants,
    thermal_coal_capacity_kw,
    thermal_lng_plants,
    thermal_lng_capacity_kw,
    thermal_oil_plants,
    thermal_oil_capacity_kw,
    thermal_lpg_plants,
    thermal_lpg_capacity_kw,
    thermal_other_gas_plants,
    thermal_other_gas_capacity_kw,
    thermal_bituminous_plants,
    thermal_bituminous_capacity_kw,
    thermal_other_plants,
    thermal_other_capacity_kw,
    thermal_plants,
    thermal_capacity_kw,
    nuclear_plants,
    nuclear_capacity_kw,
    wind_plants,
    wind_capacity_kw,
    solar_plants,
    solar_capacity_kw,
    geothermal_plants,
    geothermal_capacity_kw,
    biomass_plants,
    biomass_capacity_kw,
    waste_plants,
    waste_capacity_kw,
    storage_battery_plants,
    storage_battery_capacity_kw,
    new_energy_plants,
    new_energy_capacity_kw,
    other_plants,
    other_capacity_kw,
    total_plants,
    total_capacity_kw,
    published_as_of
from {{ ref('raw_power_plants_operator') }}
